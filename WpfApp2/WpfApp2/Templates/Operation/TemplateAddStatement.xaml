<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WpfApp2.Templates">
    <DataTemplate x:Key="TemplateAddStatement">
        <Grid Margin="0,80,0,0">
            <Label x:Name="label" Content="Составление выписки" HorizontalAlignment="Left" Margin="397,10,0,0" VerticalAlignment="Top" FontSize="24"/>
            <Button x:Name="buttond_Copy1" Content="Составить выписку" Command="{Binding Path=ToCreateStatementCommand}" HorizontalAlignment="Left" Margin="689,541,0,0" VerticalAlignment="Top"  
                    Width="300" Height="43" FontSize="22"/>
            
            <TextBlock Text=" Рекомендовано: дебанражирование и УЗ-контроль на" Width="auto" Height="23" Margin="0,0,153,420"></TextBlock>
            <TextBox Text="{Binding Path=days}" Width="40" Height="23" Margin="0,0,373,425">
            </TextBox>
            <TextBlock Margin="0,0,230,425" Width="100" Height="20" Text="сутки"></TextBlock>
            <TextBlock Text="Первая нога" Width="200" Height="23" Margin="0,0,820,320"></TextBlock>
            <ComboBox ItemsSource="{Binding Path=LeftOrRight}" SelectedIndex="{Binding Path=SelectedLeg}"  Width="140" Height="13" Margin="0,0,703,330"></ComboBox>
            <TextBlock Text="Врач " Width="200" Height="23" Margin="0,0,820,220"></TextBlock>

            <ComboBox ItemsSource="{Binding Path=Doctors}" SelectedIndex="{Binding Path=SelectedDoctor}"  Width="100" Height="13" Margin="0,0,750,230"></ComboBox>
            <Button x:Name="button_Copy" Command="{Binding Path=ToOperationOverviewCommand}" Content="Вернуться к операции" HorizontalAlignment="Left" Margin="69,541,0,0"              
                    VerticalAlignment="Top" Width="300" Height="46" FontSize="22"/>
          
        </Grid>
    </DataTemplate>
</ResourceDictionary>